import React, { useEffect, useState } from 'react';
import AOS from 'aos';
import 'aos/dist/aos.css';
import useAuth from '../../hooks/useAuth';
import useTheme from '../../hooks/useTheme';
import api from '../../services/api';
import { showError } from '../../services/alerts';

const tabs = [{ key: 'all', label: 'All' }, { key: 'admin', label: 'Admin' }, { key: 'barangay', label: 'Barangay' }, { key: 'user', label: 'User' }, { key: 'superadmin', label: 'SuperAdmin' }];
const relativeTime = (date) => { const seconds = Math.max(0, Math.floor((Date.now() - new Date(date).getTime()) / 1000)); if (seconds < 60) return 'just now'; if (seconds < 3600) return `${Math.floor(seconds / 60)} minutes ago`; if (seconds < 86400) return `${Math.floor(seconds / 3600)} hours ago`; return `${Math.floor(seconds / 86400)} days ago`; };
const pageNumbers = (pages, page) => { if (pages <= 7) return Array.from({ length: pages }, (_, index) => index + 1); const values = [1]; if (page > 3) values.push('...'); for (let value = Math.max(2, page - 1); value <= Math.min(pages - 1, page + 1); value += 1) values.push(value); if (page < pages - 2) values.push('...'); values.push(pages); return values; };

const SuperAdminActivityPage = () => {
  const { token } = useAuth(); const { theme } = useTheme(); const isDark = theme === 'dark';
  const [activities, setActivities] = useState([]); const [pagination, setPagination] = useState({ page: 1, limit: 20, total: 0, pages: 1 }); const [activeTab, setActiveTab] = useState('all'); const [search, setSearch] = useState(''); const [dateFilter, setDateFilter] = useState('all'); const [loading, setLoading] = useState(true); const [page, setPage] = useState(1);
  useEffect(() => { setPage(1); }, [activeTab, search, dateFilter]);
  useEffect(() => { AOS.init({ duration: 500, once: true }); let cancelled = false; const params = { page, limit: 20, role: activeTab === 'all' ? undefined : activeTab, search: search || undefined }; if (dateFilter !== 'all') { const start = new Date(Date.now() - Number(dateFilter) * 86400000); params.startDate = start.toISOString().slice(0, 10); } setLoading(true); api.get('/activity/all', { params, headers: { Authorization: `Bearer ${token}` } }).then((response) => { if (!cancelled) { setActivities(response.data?.entries || response.data?.activities || []); setPagination(response.data?.pagination || { page, limit: 20, total: 0, pages: 1 }); } }).catch((error) => { if (!cancelled) showError(error.response?.data?.message || 'Unable to load activity.'); }).finally(() => { if (!cancelled) setLoading(false); }); return () => { cancelled = true; }; }, [token, page, activeTab, search, dateFilter]);
  const first = pagination.total ? (pagination.page - 1) * pagination.limit + 1 : 0; const last = Math.min(pagination.page * pagination.limit, pagination.total);
  return <main className="space-y-6"><div><p className={`text-xs uppercase tracking-[0.25em] ${isDark ? 'text-gray-400' : 'text-slate-500'}`}>Audit trail</p><h1 className={`mt-2 text-2xl font-bold ${isDark ? 'text-white' : 'text-slate-900'}`}>System activity</h1></div><div className="flex flex-col gap-3 sm:flex-row"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search actor or action" className={`flex-1 rounded-lg border px-3 py-2 text-sm ${isDark ? 'border-[#2e303a] bg-[#14151d] text-white' : 'border-slate-200 bg-white text-slate-900'}`} /><select value={dateFilter} onChange={(event) => setDateFilter(event.target.value)} className={`rounded-lg border px-3 py-2 text-sm ${isDark ? 'border-[#2e303a] bg-[#14151d] text-white' : 'border-slate-200 bg-white text-slate-900'}`}><option value="all">All time</option><option value="1">Today</option><option value="7">Last 7 days</option><option value="30">Last 30 days</option></select></div><div className={`flex flex-wrap gap-2 border-b ${isDark ? 'border-[#2e303a]' : 'border-slate-200'}`}>{tabs.map((tab) => <button key={tab.key} type="button" onClick={() => setActiveTab(tab.key)} className={`border-b-2 px-3 py-3 text-sm font-medium ${activeTab === tab.key ? 'border-[#3b82f6] text-[#3b82f6]' : `border-transparent ${isDark ? 'text-gray-400' : 'text-slate-500'}`}`}>{tab.label}</button>)}</div><div className={`rounded-2xl border shadow-xl ${isDark ? 'border-[#2e303a] bg-[#14151d]' : 'border-slate-200 bg-white'}`}>{loading ? <p className={`p-8 text-center ${isDark ? 'text-gray-400' : 'text-slate-500'}`}>Loading activity...</p> : !activities.length ? <p className={`p-8 text-center ${isDark ? 'text-gray-400' : 'text-slate-500'}`}>No activity recorded yet.</p> : <div className={`max-h-[calc(100vh-320px)] overflow-y-auto divide-y ${isDark ? 'divide-[#2e303a]' : 'divide-slate-200'}`}>{activities.map((activity) => <div key={activity._id} data-aos="fade-up" className="flex flex-col gap-2 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className={isDark ? 'text-white' : 'text-slate-900'}>{activity.message || activity.action}</p><span className="mt-1 inline-flex rounded-full bg-blue-500/10 px-2 py-1 text-xs capitalize text-blue-400">{activity.actorRole || activity.role}</span></div><time title={new Date(activity.createdAt).toLocaleString()} className={`text-xs ${isDark ? 'text-gray-500' : 'text-slate-400'}`}>{relativeTime(activity.createdAt)}</time></div>)}</div>}</div><div className="flex flex-wrap items-center justify-between gap-3 px-1 py-3"><p className={`text-sm ${isDark ? 'text-gray-400' : 'text-slate-500'}`}>Showing {first}–{last} of {pagination.total} entries</p><div className="flex items-center gap-3 text-sm"><button type="button" disabled={page === 1} onClick={() => setPage((value) => value - 1)} className="text-gray-400 disabled:cursor-not-allowed disabled:opacity-40">Previous</button>{pageNumbers(pagination.pages, page).map((value, index) => value === '...' ? <span key={`ellipsis-${index}`} className="text-gray-500">...</span> : <button type="button" key={value} onClick={() => setPage(value)} className={page === value ? 'font-bold text-[#3b82f6]' : 'text-gray-400 hover:text-white'}>{value}</button>)}<button type="button" disabled={page >= pagination.pages} onClick={() => setPage((value) => value + 1)} className="text-gray-400 disabled:cursor-not-allowed disabled:opacity-40">Next</button></div></div></main>;
};
export default SuperAdminActivityPage;
