import React, { useEffect, useState } from 'react';
import useAuth from '../../hooks/useAuth';
import useTheme from '../../hooks/useTheme';
import { showError, showSuccess } from '../../services/alerts';

const defaults = { theme: 'system', emailStatus: true, emailComment: true, emailAssignment: true, language: 'English' };
const BarangaySettingsPage = () => {
  const { user, updateProfile, changePassword } = useAuth();
  const { theme } = useTheme();
  const isDark = theme === 'dark';
  const [profile, setProfile] = useState({ name: '', email: '' });
  const [passwords, setPasswords] = useState({ current: '', next: '', confirm: '' });
  const [preferences, setPreferences] = useState(defaults);
  const [saving, setSaving] = useState(false);
  const userKey = user?._id || user?.id || user?.email || 'unknown';
  useEffect(() => setProfile({ name: user?.name || '', email: user?.email || '' }), [user?.email, user?.name]);
  useEffect(() => { try { const saved = localStorage.getItem(`hazardwatch_preferences_${userKey}`); if (saved) setPreferences({ ...defaults, ...JSON.parse(saved) }); } catch { /* use defaults */ } }, [userKey]);
  const saveProfile = async (event) => { event.preventDefault(); setSaving(true); try { await updateProfile(profile.name, profile.email); await showSuccess('Profile updated successfully.'); } catch (error) { await showError(error.message || 'Unable to update profile.'); } finally { setSaving(false); } };
  const savePassword = async (event) => { event.preventDefault(); if (passwords.next !== passwords.confirm) { await showError('New passwords do not match.'); return; } setSaving(true); try { await changePassword(passwords.current, passwords.next); setPasswords({ current: '', next: '', confirm: '' }); await showSuccess('Password changed successfully.'); } catch (error) { await showError(error.message || 'Unable to change password.'); } finally { setSaving(false); } };
  const savePreferences = async (event) => { event.preventDefault(); localStorage.setItem(`hazardwatch_preferences_${userKey}`, JSON.stringify(preferences)); await showSuccess('Preferences saved successfully.'); };
  const input = `w-full rounded-xl border px-3 py-2.5 ${isDark ? 'border-[#2e303a] bg-[#0a0b0f] text-white' : 'border-slate-200 bg-slate-50 text-slate-900'}`;
  const panel = isDark ? 'border-[#2e303a] bg-[#14151d]' : 'border-slate-200 bg-white';
  const preferenceRows = [['emailStatus', 'Email on status change'], ['emailComment', 'Email on comment'], ['emailAssignment', 'Email on assignment']];
  return <div className="grid gap-6 xl:grid-cols-2"><section className={`rounded-2xl border p-5 shadow-xl ${panel}`}><p className="text-xs uppercase tracking-[0.25em] text-[#3b82f6]">{user?.barangay} settings</p><h1 className={`mt-2 text-2xl font-bold ${isDark ? 'text-white' : 'text-slate-900'}`}>Account settings</h1><form onSubmit={saveProfile} className="mt-6 space-y-4"><label className="block text-sm"><span className="mb-2 block">Full name</span><input value={profile.name} onChange={(event) => setProfile((current) => ({ ...current, name: event.target.value }))} className={input} /></label><label className="block text-sm"><span className="mb-2 block">Email</span><input type="email" value={profile.email} onChange={(event) => setProfile((current) => ({ ...current, email: event.target.value }))} className={input} /></label><button type="submit" disabled={saving} className="rounded-lg bg-[#3b82f6] px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">Save profile</button></form><form onSubmit={savePassword} className="mt-8 space-y-4 border-t border-[#2e303a] pt-6"><h2 className="text-lg font-semibold">Change password</h2>{[['current', 'Current password'], ['next', 'New password'], ['confirm', 'Confirm new password']].map(([key, label]) => <input key={key} type="password" placeholder={label} value={passwords[key]} onChange={(event) => setPasswords((current) => ({ ...current, [key]: event.target.value }))} className={input} />)}<button type="submit" disabled={saving} className="rounded-lg border border-[#3b82f6] px-4 py-2 text-sm font-semibold text-[#60a5fa] disabled:opacity-50">Change password</button></form></section><form onSubmit={savePreferences} className={`rounded-2xl border p-5 shadow-xl ${panel}`}><p className="text-xs uppercase tracking-[0.25em] text-gray-400">Preferences</p><h2 className={`mt-2 text-2xl font-bold ${isDark ? 'text-white' : 'text-slate-900'}`}>Notifications and display</h2><label className="mt-5 block text-sm">Theme preference<select value={preferences.theme} onChange={(event) => setPreferences((current) => ({ ...current, theme: event.target.value }))} className={`${input} mt-2`}><option>Dark</option><option>Light</option><option value="system">System</option></select></label><div className="mt-5 space-y-3">{preferenceRows.map(([key, label]) => <label key={key} className={`flex items-center justify-between rounded-xl border px-3 py-3 ${isDark ? 'border-[#2e303a] bg-[#0a0b0f]' : 'border-slate-200 bg-slate-50'}`}><span>{label}</span><input type="checkbox" checked={preferences[key]} onChange={(event) => setPreferences((current) => ({ ...current, [key]: event.target.checked }))} className="h-4 w-4 accent-blue-500" /></label>)}</div><label className="mt-5 block text-sm">Language preference<select value={preferences.language} onChange={(event) => setPreferences((current) => ({ ...current, language: event.target.value }))} className={`${input} mt-2`}><option>English</option><option>Filipino</option></select></label><button type="submit" className="mt-6 rounded-lg bg-[#3b82f6] px-4 py-2 text-sm font-semibold text-white">Save preferences</button></form></div>;
};

export default BarangaySettingsPage;
